# Product catalog prototype

Canonical tool documentation lives in:

```text
docs/tools/catalog.rst
```

Quick example:

```sh
python3 tools/catalog/catalog_products.py ~/goes-data --database ~/goes-data/catalog.sqlite --summary
```
