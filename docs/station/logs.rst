Logs
====

The Logs tab displays live combined output from supervised processes.

Current behavior:

- Captured stdout and stderr are appended to per-process log files.
- The live Logs tab shows combined output.
- A process-name filter controls which live entries are shown.

The application data log directory is shown in the UI. Future work can add
historical log loading, search, severity detection, and log rotation.
